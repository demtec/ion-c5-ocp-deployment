{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ion-c5.serviceAccountName" . }}
  labels:
    {{- include "ion-c5.labels" . | nindent 4 }}
{{- end }}
